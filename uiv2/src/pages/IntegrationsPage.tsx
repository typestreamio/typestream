import { Box, Typography } from '@mui/material';

export function IntegrationsPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" sx={{ mb: 3 }}>
        Integrations
      </Typography>

      <Typography variant="body1" color="text.secondary">
        Connector configuration UI will appear here (Phase 6)
      </Typography>
    </Box>
  );
}
